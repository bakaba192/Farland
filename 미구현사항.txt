승리판정 및 승리화면 -> 완료

세이브 슬롯 번호 프린트 -> 아 깜빡함..

졸개들 도는장면 이상함. -> 처리된듯

경험치 및 레벨업 -> 이거 해야됨★★★ 됨 ♡

시작화면에서 그냥시작 및 로드 -> 안해시발..

게임 종료버튼 -> 구지 필요해?

사운드는 일단 보류..


레벨업 처리

경험치 바 -> 스테이트바와 배경 

승리시 캐릭터 레벨과 경험치를 캐릭터 데이터에 저장. -> 됨
슬롯에는 클리어 정보와 현재 스테이지가 저장되어야 함. -> 됨

gainExp -> 이번턴에 얻는 경험치

totalExp -> 캐릭터가 가지고 있던 경험치 + gainExp


if(캐릭터 Exp ++ ( until totalExp))
	if(캐릭터 Exp == 100)
	{
		캐릭터 Exp = 0 ; total Exp -100;
		캐릭터
	}

그렇다면.. 경험치는 언제 얻어지는가?

지금까지 상태변화 경로

일반공격의 경우 : 이동범위 표시 ->공격범위 표시-> 공격-> 데미지 표시-> (사망) -> (경험치처리) ->논

스킬공격의 경우 : 스킬사거리 표시 -> 스킬범위 표시-> 공격 -> 데미지 표시 -> (사망) -> (경험치처리) ->논



자.. 그럼..

1. 공격 경험치를 얻는다.

2. 처치 경험치를 얻는다.

3. 두 경험치를 합해 gainExp를 얻는다

4. gainExp와 CharaExp를 합해 totalExp를 얻는다.

5. 상태를 gainExp로 바꾼다.

6-1. 상태가 gainExp면 render에서 경험치 획득창과 경험치 바를 그린다.

6-2. 상태가 gainExp면 if(!gainExp()) 호출 true이면 none으로